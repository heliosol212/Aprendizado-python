#variável: um valor definido que pode sofrer alterações de acordo o codigo do programa
#Exemplo:
a = int(10)

if (a == 10):
    print(a, "Somado a 5 fica:", a + 5) 
else: 
    print("fake")

#ou com input do usuário:

d = int(input("Digite o valor de b"))
def somarcinco(valor):
    return valor + 5

resultado = somarcinco(d)
print(resultado)

#constantes são valores que não mudam, variam. Vão ter o mesmo valor do inicio ao fim do programa.
#entre tanto, não é possível declarar uma variável como constant. Todas são variáveis.

#já que não é possível declarar uma constante com o python em si
#deixe as variáveis que você considera constantes em letras maiusculas
#Exemplo:

b = 20 #Variável comúm
B = 21 #variável constante

#snake case:
#substituir todos os espaços em brancos por "_" ou seja: "snake_case"

#exemplo:

arroz_com_feijao = str("é bom para saúde porém eu odeio feijão.")
#use nomes sugestivos para encurtar os nomes das variáveeis

variavel_qualquer = 10
var_qulq = 10 


